{{#if loggedIn}}
<section class="home-card card">
    <a href="/home/post/{{id}}">
    <div class="card-header  ">
        <h1 class="card-title">{{title}}</h1>
        </div>
        <div class="card-body">
            <p>{{description}}</p>  
        
        </div>
        </a>
</section>
{{/if}}