
<section>
    <div class="box-template">
        <a href="/home/post/{{id}}">
       <div class="header-template">
           <h1 class="align-header">{{title}}</h1>
            <p class="align-header " id="username"> Posted by: {{user.username}}</p>
       </div>
       <div class="card-content">
           <p>{{description}}</p>
       </div>
       </a>
    </div>
    
  
</section>
