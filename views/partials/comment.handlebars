
    <div class="box-template">
        <div class="header-template">
         <a>  
        </div>
        <div class="card-content">
            <p>{{comment}}</p>
            <p class="align-header " id="username">-{{user.username}}, {{format_date date_published}}</p>
           
        </div>
        </a> 
    </div>
